function data = sparse_data(data,sparse_ind,start_ind)
%This function is use to generate sparse data copy that used for plotting
if nargin<2 || isempty(sparse_ind)
    sparse_ind=1;
end

if nargin<3 || isempty(start_ind)
   start_ind=1; 
end
for

for data_ind=start_ind:length(data_ind)
    data{data_ind}.x
end


end

